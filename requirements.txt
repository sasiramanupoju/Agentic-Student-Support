# Student Support System - Dependencies (Cloud-Optimised for Vercel)

# LangChain Core
langchain>=0.1.0
langchain-core>=0.1.0
langchain-community>=0.0.38
langchain-groq>=0.0.1
langgraph>=0.0.20
langchain-text-splitters>=0.0.1

# Cloud Vector Store & Embeddings
# REMOVED: faiss-cpu, sentence-transformers, chromadb (caused 7.5GB bundle — exceeded Vercel limit)
pinecone>=3.0.0                  # Cloud vector database — replaces local ChromaDB/FAISS
langchain-pinecone>=0.1.0        # LangChain integration for Pinecone
langchain-huggingface>=0.0.1     # LangChain integration for HuggingFace Inference API (cloud embeddings)
huggingface-hub>=0.20.0          # HuggingFace client — replaces 5GB local sentence-transformers

# Cloud Database (Supabase Postgres)
# REMOVED: local SQLite is wiped on every Vercel cold start
psycopg2-binary>=2.9.9           # Postgres driver for Supabase connection

# Email Service
sendgrid>=6.10.0

# LLM API
groq>=0.4.0

# Flask Web Interface
flask>=2.3.0
flask-cors>=4.0.0

# Authentication
PyJWT>=2.8.0
Werkzeug>=3.0.0
argon2-cffi>=23.1.0

# Chat Memory (Upstash Redis — serverless, replaces local SQLite chat_memory.db)
upstash-redis>=1.0.0

# Excel Import (for bulk data import via the admin panel)
openpyxl>=3.1.0

# Utilities
pytz>=2023.3
python-dotenv>=1.0.0
